{
	"name": "fountain-lsp-client",
	"private": true,
	"description": "Fountain Language Server Client",
	"author": "Oliver Paraskos",
	"license": "MIT",
	"version": "0.0.1",
	"publisher": "vscode",
	"scripts": {
		"test": "echo noop",
		"watch": "npm run build -- --watch",
		"build": "esbuild ./src/extension.ts --bundle --outfile=dist/main.js --external:vscode --format=cjs --platform=node --sourcemap"
	},
	"engines": {
		"vscode": "^1.63.0"
	},
	"dependencies": {
		"vscode-languageclient": "^7.0.0"
	},
	"devDependencies": {
		"@types/vscode": "^1.63.0",
		"@vscode/test-electron": "^2.1.2",
		"esbuild": "^0.15.10"
	}
}
